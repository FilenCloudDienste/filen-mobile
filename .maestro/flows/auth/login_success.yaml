appId: io.filen.app
name: "Auth: User can login with correct credentials"
tags:
  - auth
env:
  EMAIL: ${EMAIL}
  PASSWORD: ${PASSWORD}
---
- runFlow:
    file: ../login.yaml
    env:
      EMAIL: ${EMAIL}
      PASSWORD: ${PASSWORD}