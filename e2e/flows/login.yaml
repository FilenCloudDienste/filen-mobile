appId: io.filen.app
env:
  EMAIL: ${EMAIL}
  PASSWORD: ${PASSWORD}
---
- launchApp:
    clearState: true
- tapOn: Log in
- tapOn:
    id: email
- inputText: 
    label: Enter the test user's email
    text: ${EMAIL}
- tapOn:
    id: password
- inputText:
    label: Enter the test user's password
    text: ${PASSWORD}
- assertVisible:
    text: Log in
    enabled: true
- tapOn:
    id: login
- tapOn:
    text: Later
    optional: true
- assertVisible:
    text: Home
